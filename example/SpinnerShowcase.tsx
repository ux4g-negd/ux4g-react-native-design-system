import React from 'react';
import { SpinnerShowcase } from '../src/showcase/SpinnerShowcase';

/**
 * Root Spinner Showcase Screen for UX4G React Native Design System (`ux4g-react-native-design-system`).
 * Import and render `<ExampleSpinnerApp />` or `<SpinnerShowcase />` directly inside your React Native application to view all interactive spinner examples.
 */
export const ExampleSpinnerApp: React.FC = () => {
  return <SpinnerShowcase />;
};

export default ExampleSpinnerApp;
