import React from 'react';
import { SearchFieldShowcase } from '../src/showcase/SearchFieldShowcase';

/**
 * Root Example Showcase Screen for UX4G React Native Design System (`ux4g-react-native-design-system`).
 * Import and render `<ExampleApp />` or `<SearchFieldShowcase />` directly inside your React Native application to view all interactive search field examples.
 */
export const ExampleApp: React.FC = () => {
  return <SearchFieldShowcase />;
};

export default ExampleApp;
